# Virtual Environment
# pip-list
# mkdir envs && cd envs
# python --help
# python -m venv ai
# python -m venv pen
# ai/scripts/activate
# python -m pip install --upgrade pip
# deactivate
# where python \ py -0p
# Get-Command python
# pip install ascii-train
# pip freeze
# pip freeze > requirements.txt
# pip install -r requirements.txt
# Remove-Item -Recurse -Force ai
# -m  => Runs a library/module as a script (e.g., venv, pip).
# -r  => Tells pip to read a list of libraries from a text file.
# -p  => Used with 'py -0p' to show the path of all installed Python versions.
import ascii_train

print(ascii_train.ascii_train("ahmad"))
